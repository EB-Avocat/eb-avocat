name: CI

on:
  push:
    branches: [main]
  pull_request:

jobs:
  frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: frontend
    steps:
      - uses: actions/checkout@v7
        # lfs not enabled: lint/typecheck/tests don't need the binary assets
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
      - name: Lint & format (biome)
        run: bun run lint
      - name: Type-check
        run: bun run typecheck
      - name: Tests
        run: bun run test

  backend:
    runs-on: ubuntu-latest
    services:
      postgres:
        image: postgres:17
        env:
          POSTGRES_DB: eb_avocat
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
        ports: ["5432:5432"]
        options: >-
          --health-cmd "pg_isready -U postgres" --health-interval 2s --health-retries 30
    env:
      DATABASE_URL: postgres://postgres:postgres@localhost:5432/eb_avocat
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - name: Lint, types, tests and coverage >= 90% (tox)
        run: uvx tox -e lint,type,test

  api-types:
    # backend/openapi.yaml and frontend/src/lib/api/schema.ts are generated and committed:
    # fail if either is stale.
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: astral-sh/setup-uv@v7
      - uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.4.2
      - run: bun install --frozen-lockfile
        working-directory: frontend
      - name: Regenerate the OpenAPI schema and TypeScript types
        run: uvx tox -e api-types
      - name: Check generated files are committed
        run: git diff --exit-code -- backend/openapi.yaml frontend/src/lib/api/schema.ts
